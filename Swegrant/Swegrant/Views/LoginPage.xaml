<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Swegrant.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <RadioButton x:Name="rdAdminUser" Content="Admin User" IsChecked="{Binding IsAdmin}" />
            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                <Label Text="Password :"/>
                <Entry IsPassword="True" Text="{Binding AdminPass}" IsEnabled="{Binding IsAdmin}" HorizontalOptions="FillAndExpand"/>
            </StackLayout>
            <RadioButton x:Name="rdNormalUser" Content="Normal User"  IsChecked="{Binding IsNormal}"  />

            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>